import { CreateEventForm } from "@/components/create-event-form";

export const metadata = { title: "Create an event — VibePass" };

export default function NewEventPage() {
  return <CreateEventForm />;
}
